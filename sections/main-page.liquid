<div class="section" data-section-id="{{ section.id }}">
  <div class="page-width page-width--narrow">
    <h1 style="margin-bottom: var(--sp-6);">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</div>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
